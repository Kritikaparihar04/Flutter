//  FOR ROW PROPERTY

//mainAxisAlignment decides the space between the modules horizontally
//crossAxisAlignment decides the alignment vertically

/*
class Home extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("this is our first app"),
        centerTitle: true,
        backgroundColor: Colors.pink[200],
      ),
      body: Row(
        mainAxisAlignment: MainAxisAlignment.spaceEvenly,
        crossAxisAlignment: CrossAxisAlignment.end,
        children: [
          Container(
            color: Colors.greenAccent,
            child: Text("I m container"),
          ),
          FloatingActionButton(
            onPressed: () {},
            child: Text("click"),
            backgroundColor: Colors.pink[400],
          ),
          Text('oh yaa this is the text section')
        ],
      ),
    );
  }
}
*/

//FOR COLUMNS
/*
class Home extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("this is our first app"),
        centerTitle: true,
        backgroundColor: Colors.pink[200],
      ),
      body: Column(
        mainAxisAlignment: MainAxisAlignment.spaceEvenly,
        crossAxisAlignment: CrossAxisAlignment.stretch,
        children: <Widget>[
          Row(children: [
            Text("Kritika"),
            Text("Parihar"),
            Column(
              children: [
                Container(
                  color: Colors.blue,
                  child: Text("I m container"),
                  padding: EdgeInsets.all(30.0),
                ),
              ],
            )
          ]),
          Container(
            color: Colors.greenAccent,
            child: Text("I m container"),
            padding: EdgeInsets.all(30.0),
          ),
          Container(
            color: Colors.amber,
            padding: EdgeInsets.all(30.0),
            child: Text("I m container"),
          ),
          Container(
            color: Colors.pink,
            padding: EdgeInsets.all(30.0),
            child: Text("I m container"),
          ),
        ],
      ),
    );
  }
}

*/
//EXPANDED WIDGETS
/*
class Home extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("this is our first app"),
          centerTitle: true,
          backgroundColor: Colors.pink[200],
        ),
        body: Row(
          children: [
            Expanded(
              //flex property decide what portion of size it will get
              flex: 3,
              child: Container(
                color: Colors.pink,
                padding: EdgeInsets.all(20.0),
                child: Text('1'),
              ),
            ),
            Expanded(
              flex: 2,
              child: Container(
                color: Colors.amber,
                padding: EdgeInsets.all(20.0),
                child: Text('1'),
              ),
            ),
            Expanded(
              flex: 1,
              child: Container(
                color: Colors.blue,
                padding: EdgeInsets.all(20.0),
                child: Text('1'),
              ),
            ),
          ],
        )
        );

  }
}

*/
