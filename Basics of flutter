import 'package:flutter/material.dart';

void main() => runApp(MaterialApp(
      home: Home(),
    ));
/* 
FOR TEXT
-------------------------------------------------------
class Home extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("this is our first app"),
        centerTitle: true,
        backgroundColor: Colors.pink[200],
      ),
      body: Center(
        child: Text(
          "this is in center",
          style: TextStyle(
              fontSize: 20.0,
              fontWeight: FontWeight.bold,
              letterSpacing: 2.0,
              color: Colors.green[400],
              fontFamily: 'IndieFlower'),
        ),
      ),
      floatingActionButton: FloatingActionButton(
        onPressed: () {},
        child: Text("click"),
        backgroundColor: Colors.pink[400],
      ),
    );
  }
}
*/
/*
//FOR IMAGE
class Home extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("this is our first app"),
        centerTitle: true,
        backgroundColor: Colors.pink[200],
      ),
      body: Center(
        child: Image.asset('assests/img2.jpg'),

        // AssetImage('assests/img1.jpg'),
        /*
          image: NetworkImage(
              'https://tse4.mm.bing.net/th?id=OIP.LyicL9CIJVBQvN5ipWNKhwHaEo&pid=Api&P=0&w=254&h=159'),*/
      ),
      floatingActionButton: FloatingActionButton(
        onPressed: () {},
        child: Text("click"),
        backgroundColor: Colors.pink[400],
      ),
    );
  }
}
//assests/   is used for saving all the images present in the assets folder otherwise u have to save the images one by one
*/

//FOR ICONS
/*
class Home extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("this is our first app"),
        centerTitle: true,
        backgroundColor: Colors.pink[200],
      ),
      body: Center(
        child: Icon(
          Icons.car_rental,
          color: Colors.pink,
          size: 50.0,
        ),
      ),
      floatingActionButton: FloatingActionButton(
        onPressed: () {},
        child: Text("click"),
        backgroundColor: Colors.pink[400],
      ),
    );
  }
}
*/

//RAISED BUTTON noq is called Elevated Button
/*

class Home extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("this is our first app"),
        centerTitle: true,
        backgroundColor: Colors.pink[200],
      ),
      body: Center(
        child: ElevatedButton(
          onPressed: () {
            print("OMG! You clicked ME...AWeee");
          },
          child: Text("its up"),
        ),
      ),
      floatingActionButton: FloatingActionButton(
        onPressed: () {},
        child: Text("click"),
        backgroundColor: Colors.pink[400],
      ),
    );
  }
}
*/

//NOW WE WILL USE ICONS AND TEXT INSIDE THE ELEVATED BUTTON

/*
class Home extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("this is our first app"),
        centerTitle: true,
        backgroundColor: Colors.pink[200],
      ),
      body: Center(
        child: ElevatedButton.icon(
          onPressed: () {},
          icon: Icon(Icons.mail),
          label: Text("Mail me"),
          
        ),
      ),
      floatingActionButton: FloatingActionButton(
        onPressed: () {},
        child: Text("click"),
        backgroundColor: Colors.pink[400],
      ),
    );
  }
}
*/

class Home extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("this is our first app"),
        centerTitle: true,
        backgroundColor: Colors.pink[200],
      ),
      body: Center(
        child: IconButton(
          onPressed: () {
            print('you clicked me');
          },
          icon: Icon(Icons.alternate_email),
          color: Colors.yellow,
        ),
      ),
      floatingActionButton: FloatingActionButton(
        onPressed: () {},
        child: Text("click"),
        backgroundColor: Colors.pink[400],
      ),
    );
  }
}
